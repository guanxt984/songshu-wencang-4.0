# 松鼠文仓 产品文档

## 1. 产品定位

松鼠文仓是一款面向学习者、内容创作者、产品经理与知识工作者的 AI 知识整理工具，帮助用户把从文章、课程、聊天、网页和笔记中复制来的碎片化信息，整理成结构清晰、可继续补充、可复盘阅读的结构化文档。

产品本质不是“摘要工具”，而是一个“碎片化信息到结构化文档”的知识组织系统：用户负责收集原始材料，产品负责识别主题、建立结构、沉淀文档，并保留原始信息与结构化内容之间的对应关系。

## 2. 目标用户与核心场景

### 目标用户

- 学生、求职者、产品经理、研究者等需要长期整理资料的人。
- 经常从多处搜集知识，但难以把碎片材料整理成可阅读文档的人。
- 希望用 AI 辅助知识整理，但仍需要保留人工编辑、复盘和二次组织空间的人。

### 核心场景

用户持续收集零散信息，将其作为“松果”放入某个“松鼠仓”；当材料积累到一定程度后，产品将松果整理为文档章节，并在右侧“松果架”中保留每个章节对应的原始松果，方便用户追溯、修改和继续扩展。

## 3. 核心概念

| 概念 | 定义 | 产品作用 |
|---|---|---|
| 松鼠仓 | 一个主题知识库 | 承载某个主题下的文档、松果架和原始松果 |
| 松果 | 用户粘贴进来的碎片化信息 | 作为结构化文档的原材料 |
| 暂存栏 | 尚未整理进章节的松果集合 | 承接新增材料，避免输入后立即改变文档结构 |
| 松果架 | 按章节组织的松果集合 | 展示文档章节与原始材料之间的对应关系 |
| 结构化文档 | AI/系统整理后的阅读内容 | 面向复盘、阅读和继续编辑 |
| 精选松果 | 重要原始信息 | 作为重点引用或关键论据保留 |

## 4. 功能设计

### 4.1 松鼠仓管理

- 支持多个松鼠仓并列管理，每个松鼠仓代表一个独立主题。
- 支持新建、删除、切换和拖拽排序。
- 每个松鼠仓拥有独立的文档、松果架、松果和图标，避免不同主题数据混乱。

### 4.2 松果输入与暂存

- 用户可以通过底部工具栏添加松果。
- 新松果默认进入暂存栏，不立即影响正式文档结构。
- 用户也可以选择直接放入已有松果架，或新建松果架后放入。

### 4.3 结构化文档区

- 文档区负责展示整理后的结构化内容。
- 左侧目录与文档章节联动，点击目录可跳转至对应章节。
- 章节内容包含标题、摘要和重点要点。
- 支持编辑文档内容，方便用户在 AI 整理后继续人工修订。

### 4.4 松果架

- 松果架用于展示每个文档章节背后的原始松果。
- 展开后呈现为右侧固定大面板，面积约为文档区的 2/3。
- 顶部栏与架体融为一体，支持点击标题区域折叠。
- 松果架第一栏为暂存栏，其后按文档章节一一对应展示。
- 每个架子标题行展示“架子名称 + 松果数量”，并提供统一“修改”入口。
- 松果默认只展示内容；移动、编辑、删除等操作收纳在“修改”状态中，降低信息噪音。
- 松果卡片按内容长度自适应高度，以更紧凑的瀑布流方式展示。

### 4.5 底部工具栏

- 保留三个主操作：添加松果、编辑文档、全部重新整理。
- 工具栏固定在文档区底部，由趴伏松鼠作为视觉锚点，强化产品记忆点。
- 工具栏按钮采用稳定三列布局，避免文字和图标错位。

### 4.6 自定义图标

- 松鼠仓支持自定义图标上传与裁剪。
- 文档标题图标与松鼠仓列表图标保持联动，形成统一身份识别。

## 5. 信息处理原则

产品处理碎片化信息时遵循以下原则：

1. 先识别主题，再组织结构，而不是简单压缩原文。
2. 优先建立层级清晰的文档骨架。
3. 保留原始松果与文档章节之间的关系，方便追溯。
4. 不要求用户提前整理材料，用户只需持续输入碎片信息。
5. AI 整理结果不是终稿，而是可编辑、可复盘、可继续扩展的知识结构。

详细原则见：[docs/fragmented-information-processing-principles.md](docs/fragmented-information-processing-principles.md)

## 6. 数据结构设计

当前数据采用按松鼠仓隔离的结构，避免不同仓库之间的文档、松果架和松果混淆。

```js
{
  version: 7,
  activeWarehouseId,
  warehouses: {
    byId,
    order
  },
  documents: {
    byWarehouseId
  },
  shelves: {
    byWarehouseId
  },
  pinecones: {
    byWarehouseId
  }
}
```

### 设计原因

- `warehouses` 只保存仓库基础信息和排序。
- `documents.byWarehouseId` 保存每个仓库对应的结构化文档。
- `shelves.byWarehouseId` 保存每个仓库对应的松果架。
- `pinecones.byWarehouseId` 保存每个仓库对应的原始松果。
- 所有读写操作都通过 `warehouseId` 定位，保证仓库之间数据隔离。

### 数据迁移

旧版本中，文档、松果架和松果直接嵌套在仓库对象内。当前版本通过 `normalizeWarehouseState` 自动迁移旧结构，并保存为新格式。

## 7. 技术实现

### 技术栈

- 前端：原生 HTML、CSS、JavaScript ES Modules。
- 本地服务：Node.js 静态服务。
- 数据存储：浏览器 `localStorage`。
- 测试：Node test runner + 自定义 UI 内容检查脚本。

### 关键文件

| 文件 | 作用 |
|---|---|
| `index.html` | 页面入口 |
| `app.js` | UI 渲染、状态管理、交互逻辑 |
| `styles.css` | 视觉样式、布局、响应式规则 |
| `warehouse-management.js` | 仓库数据操作与数据结构迁移 |
| `warehouse-management.test.mjs` | 仓库数据结构与操作测试 |
| `test-ui-content.cjs` | UI 内容与关键结构检查 |
| `server.mjs` | 本地静态服务器 |

### 关键实现

- 使用 `hydrateWarehouseRecord` 将分区存储的数据还原为单个仓库的完整视图。
- 使用 `persistWarehouseRecord` 将修改后的仓库内容写回对应分区。
- 使用 `removeWarehouseRecord` 保证删除仓库时同步删除其文档、松果架和松果。
- 使用 `reorderWarehouseRecords` 只改变仓库排序，不影响仓库内容。
- 松果架采用展开态/折叠态，通过 `shelfOpen` 控制。
- 松果架操作状态采用 `activeShelfActionId`，避免所有松果同时显示编辑入口。

## 8. 当前完成状态

### 已完成

- 松鼠仓列表与多仓库切换。
- 新建、删除、拖拽排序松鼠仓。
- 添加松果、暂存栏、已有架子和新建架子入口。
- 结构化文档展示与目录跳转。
- 文档编辑。
- 松果架展开/折叠、暂存栏展示、章节松果映射。
- 松果移动、编辑、删除。
- 自定义仓库图标。
- 工具栏趴伏松鼠视觉。
- 分区化数据结构与数据隔离。

### 待优化

- 接入真实 AI 整理能力，替换当前 mock 整理逻辑。
- 增加用户登录和云端同步。
- 增加导出 Markdown / PDF / Word。
- 增加公开分享页面。
- 增加移动端适配。
- 发布到公网并完善项目案例页。

## 9. 发布计划

### 第一阶段：可演示版本

- 保持当前本地数据能力。
- 部署静态前端页面。
- 准备演示用样例仓库和样例文档。
- 将项目作为作品集链接加入简历。

### 第二阶段：可用版本

- 接入 AI API，实现真实“碎片化信息到结构化文档”流程。
- 支持导入、导出和多文档管理。
- 增加云端数据存储和用户账号。

### 第三阶段：产品化版本

- 建立用户留存路径：持续输入、定期整理、复盘阅读。
- 支持协作、分享和公开文档。
- 建立模板系统，如面试整理、课程笔记、读书笔记、产品资料库。

## 10. 简历项目表述

### 项目名称 + 功能名称

松鼠文仓｜AI 碎片知识结构化整理系统

### 产品定义

面向知识工作者和求职学习人群，设计并实现一款将多来源碎片信息沉淀为结构化文档的 AI 知识整理工具，通过“松果—松果架—结构化文档”的信息架构，解决资料零散、难以复盘和原文不可追溯的问题。

### 高价值行动

1. 从“用户只会粘贴碎片材料”的真实输入行为出发，重新定义产品核心流程，设计“暂存栏—章节松果架—结构化文档”的信息组织模型，让用户无需预整理材料，也能逐步沉淀可阅读文档。
2. 设计并落地多仓库数据隔离架构，将仓库、文档、松果架和松果按 `warehouseId` 分区存储，解决多主题知识库之间数据混乱的问题，并支持创建、删除、排序和旧数据迁移。
3. 迭代松果架与文档区的双栏交互，将原始材料与结构化章节建立可视化映射；通过暂存栏、架子级修改、瀑布流松果展示等设计，降低信息噪音并提升复盘效率。

