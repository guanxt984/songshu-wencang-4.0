# 工具栏与文档区改造进度

## 本次目标

- 将三个文档工具固定到文档区右上角。
- 删除原有搜索、星标和更多按钮。
- 删除底部 Tab 式悬浮工具栏，避免遮挡文档。
- 松鼠独立趴在文档区上边界。
- 文档内容延伸至文档区底部并可完整滚动查看。

## 已完成

- `app.js` 已移除右上角搜索、星标、更多按钮。
- 三个真实操作已迁移到 `document-corner-tools`：添加松果、编辑/保存文档、重新整理。
- 已删除底部工具栏的渲染入口。
- 松鼠已从工具栏中拆出为独立的 `document-mascot`。
- 文档卡片底部预留从 `132px` 降至 `34px`。
- 已增加桌面端和窄屏工具按钮的基础布局。
- 已更新 UI 测试，并确认测试能够捕获尚未完成的细节。

## 用户最新标注意见

1. 松鼠应趴在文档区上方边界，不在文档区内部。
2. 文档展示部分应连接到最底部边缘，不被遮挡。
3. 松果架整体下移 `40px`；松果架及其图标左移 `5px`。
4. 右上角工具目前按键感不足，需要通过纹样、边缘和按压状态强化可点击感。

## 当前未完成项

- 为右上角工具补充纸纹差异、悬停和按压反馈。
- 将正文底部内边距调整为新的完整展示规格。
- 将松果架桌面端 `top` 从 `96px` 调整为 `136px`。
- 将展开态松果架 `right` 从 `24px` 调整为 `29px`，折叠态同步左移。
- 清理 CSS 中残留的 `.bottom-toolbar` 响应式规则。
- 清理 `app.js` 中仍存在的 `doc-search` 事件绑定或无效引用。
- 调整测试对松鼠结构的检查，使其匹配独立松鼠与工具导航的实际相邻结构。
- 刷新浏览器，分别验证桌面端、窄屏、长文档底部和松果架展开/折叠状态。

## 当前测试状态

`node test-ui-content.cjs` 处于预期的红灯阶段，剩余失败项对应上述未完成内容：

- 缺少 `.corner-tool:active`。
- 缺少 `top: 136px`。
- 缺少 `right: 29px`。
- CSS 仍含 `bottom-toolbar`。
- `app.js` 仍含 `doc-search`。
- 松鼠独立结构检查尚未满足。
- 正文底部内边距尚未改为 `32px`。

## 明日继续顺序

1. 完成上述 CSS 与遗留事件清理。
2. 运行 `node test-ui-content.cjs` 直至通过。
3. 运行 `npm.cmd run check` 完整回归。
4. 在 `http://127.0.0.1:5180/` 刷新并进行浏览器视觉验收。

